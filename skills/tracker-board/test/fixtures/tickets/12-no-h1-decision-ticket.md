<!-- expect: number=12 title=from-filename dialect=decision statusPrefix=claimed state=in-progress -->
<!-- trap: decision tickets have NO H1 template at all upstream — the question is the body and
     there is frequently no heading above it. A parser that requires an H1 to produce a title
     marks every canonical wayfinder ticket unparsed.
     Fall back to the filename slug for display. The number already comes from the filename,
     so nothing is actually lost; only the human-readable title degrades, and it degrades to
     something still readable. -->

Type: prototype (HITL)
Blocked by: 09, 10
Status: claimed

Does a single stacked view survive at 40 vintages, or does it need a vintage selector?

Build the throwaway at 3 vintages and at 40, look at both, keep the answer and delete the
code.
