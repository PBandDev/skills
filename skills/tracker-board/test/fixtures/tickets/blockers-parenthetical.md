<!-- expect: dialect=decision blockedBy=[] externalBlocker=none state=ready -->
<!-- trap: a digit-scanning regex returns [2,3]. The truth is NO blockers — the numbers sit
     inside a parenthetical explaining what USED to block this ticket. Mis-parsing this one
     line moves three cards and two lane counts: this ticket leaves the agent lane, the
     frontier drops by one, and a downstream ticket flips from Blocked to Frozen. -->

# 10 — Name the launch date

Type: grilling
Blocked by: — (was 02, 03; the vendor contact named the date on 2026-07-24 and dependency 03 is resolved)
Status: open

## Question

Which date do we commit to publicly, given the vendor feed refreshes on the 1st?
